<UserControl x:Class="OPSW11.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1000">

    <Grid Margin="32,28,32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Dziennik zdarzeń"
                       Style="{StaticResource HeadingXL}" Margin="0,0,0,4"/>
            <TextBlock Text="Historia wszystkich operacji wykonanych w tej sesji."
                       Style="{StaticResource BodyText}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardCompact}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="FILTR:"
                           Style="{StaticResource CaptionText}"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>

                <RadioButton Grid.Column="1" x:Name="FilterAll" Content="Wszystko"
                             IsChecked="True" GroupName="LogFilter"
                             VerticalAlignment="Center" Margin="0,0,8,0"
                             Checked="Filter_Changed"/>
                <RadioButton Grid.Column="2" x:Name="FilterWarning" Content="Ostrzeżenie"
                             GroupName="LogFilter"
                             VerticalAlignment="Center" Margin="0,0,8,0"
                             Checked="Filter_Changed"/>
                <RadioButton Grid.Column="3" x:Name="FilterError" Content="Błąd"
                             GroupName="LogFilter"
                             VerticalAlignment="Center" Margin="0,0,8,0"
                             Checked="Filter_Changed"/>
                <RadioButton Grid.Column="4" x:Name="FilterSuccess" Content="Sukces"
                             GroupName="LogFilter"
                             VerticalAlignment="Center"
                             Checked="Filter_Changed"/>

                <Border Grid.Column="5" Width="1"
                        Background="{DynamicResource SeparatorBrush}"
                        Margin="12,2" HorizontalAlignment="Right"/>

                <Button Grid.Column="6"
                        Content="Otwórz plik dziennika"
                        Style="{StaticResource SecondaryButton}"
                        Padding="12,6" Margin="0,0,6,0"
                        Click="OpenLogFile_Click"/>
                <Button Grid.Column="7"
                        Content="Wyczyść"
                        Style="{StaticResource SecondaryButton}"
                        Padding="12,6"
                        Click="ClearLogs_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Separator Grid.Row="0" Margin="0,0,0,4"/>

                <ListBox Grid.Row="1"
                         x:Name="LogListBox"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ItemContainerStyle="{StaticResource LogListItem}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}"
                                           FontFamily="Consolas, Courier New" FontSize="11"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           VerticalAlignment="Center"/>
                                <Border Grid.Column="1" CornerRadius="4" Padding="5,2"
                                        HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock x:Name="LevelText"
                                               Text="{Binding LevelTag}"
                                               FontSize="10" FontWeight="SemiBold"
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Success">
                                                        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                        <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                                        <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Message}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="2" Padding="4,6,4,0">
                    <TextBlock x:Name="LogCountText"
                               Text="0 wpisów"
                               Style="{StaticResource CaptionText}"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
